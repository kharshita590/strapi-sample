'use strict';

/**
 * contact-directory-nicv controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::contact-directory-nicv.contact-directory-nicv');
