'use strict';

/**
 * contact-directory-nicv router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::contact-directory-nicv.contact-directory-nicv');
