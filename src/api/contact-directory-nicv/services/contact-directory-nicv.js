'use strict';

/**
 * contact-directory-nicv service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::contact-directory-nicv.contact-directory-nicv');
