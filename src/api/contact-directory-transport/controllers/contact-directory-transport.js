'use strict';

/**
 * contact-directory-transport controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::contact-directory-transport.contact-directory-transport');
