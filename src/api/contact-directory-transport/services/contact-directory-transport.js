'use strict';

/**
 * contact-directory-transport service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::contact-directory-transport.contact-directory-transport');
