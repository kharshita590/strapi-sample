'use strict';

/**
 * contact-directory-transport router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::contact-directory-transport.contact-directory-transport');
